# Server Configuration
PORT=3000
NODE_ENV=development

# Database (use PostgreSQL for production)
# For SQLite (development): DATABASE_URL="file:./dev.db"
# For PostgreSQL (production): DATABASE_URL="postgresql://user:password@localhost:5432/hayqc"
DATABASE_URL="postgresql://user:password@localhost:5432/hayqc"

# Authentication - REQUIRED
# Generate a secure secret: openssl rand -base64 32
AUTH_SECRET="your-secure-secret-minimum-32-characters"

# Error Tracking (optional - recommended for production)
# Get your DSN from https://sentry.io
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Optional: OpenAI API Key (if using AI features)
# OPENAI_API_KEY=sk-your-key-here
